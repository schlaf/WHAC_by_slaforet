<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="WhacTheme" parent="Theme.AppCompat.Light">
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/IndianRed</item>

    <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/FireBrick</item>

    <!-- colorAccent is used as the default value for colorControlActivated,
         which is used to tint widgets -->
        <item name="colorAccent">@color/FireBrickHalfTransparent</item>

    <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight, and colorSwitchThumbNormal. -->

    </style>

    <style name="WhacThemeOld" parent="Theme.AppCompat">
    </style>
    
    <style name="WhacTheme.Medium">
        <item name="font_medium">@style/FontStyle.Medium</item>
    </style>

    <style name="StyledDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/IndianRed</item>
        <item name="colorPrimaryDark">@color/FireBrick</item>
        <item name="colorAccent">@color/FireBrickHalfTransparent</item>
    </style>

    <style name="AlertDialogCustom" parent="@android:style/Theme.Dialog">
        <item name="android:textColor">#00FF00</item>
        <item name="android:typeface">monospace</item>
        <item name="android:textSize">10sp</item>
    </style>
    
</resources>
